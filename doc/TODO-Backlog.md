# TODO-Backlog
*Durable work items: features, doctrine edits, deferred instruments. Priority-ordered, append-mostly; items die by completion or repeal, never by silence. Referenced by handoffs (which no longer carry a full copy). Sole todo instrument as of S69 (TODO-Processing.md retired; batchcuration workflow reference lives in CURATION_CONSTITUTION §9).*

2. **[DEFERRED] Qualifier-kind column — the database becomes a database** — *in plain terms: square-bracket variants like [not] are a type tag smuggled into a text value; give the type its own column and let the UI render the brackets, the way ⟨⟩ are already rendered rather than stored.* Absorbs the S69 mechanical-strata item (same column, seen from the metrics side) and the S70 schema-debt diagnosis (curator ruling: "I want the database to be a database, not a known substitute"). Spec agreed S70: (a) new nullable column `qual_kind` — values `operator` (a real qualifier: performs the subtraction) vs `variant` (annotation about form: [noun], [verb], [spelling], [synonym]…); exact register of names open, scheme is what's settled; (b) backfill `WHERE qualifier LIKE '[%]'`, strip brackets from stored values; (c) UI: the existing variant pills already ARE the user's kind-declaration — post-migration a pill tap writes kind=variant + bare value, free-typed text always writes kind=operator (closes the silent-reclassification risk), typed `[x]` normalized at save as legacy shorthand; brackets become pure presentation; (d) a visible kind-state near the field (chip or tag) per explicitness — design at migration time; (e) consider whether the enum wants a third value for other mechanical strata. Payoffs: every `isBracketQual()` regex becomes a column read; census toggle, qnav skip, Variant Review detection, qualFilter all simplify; depth/fat-family metrics get honest `WHERE kind` clauses (motivating data stands: 31% of 'make''s 644 closure edges are mechanical). Interim note unchanged: string-match works today; this is optional debt retirement, not a fire. Requires a planned Supabase sitting with the curator's hands.
8. **Dashboard enhancements** — (1) make all panels interactive — tapping a tile opens its population, likely requiring new worklists (each doctrine tile is a query wanting a face: inversions, collisions, duplicates, unreachables); (2) enlarge the supporting text (tile-desc/tile-sub); (3) move all user-facing strings to strings.js per the S53 DB_* pattern — the five doctrine tiles hardcode label/desc in HTML; (4) add the missing visible UI version stamp (S69 verification: line-1 comment only, footer has no stamp anchor — the recovered v2606072620 base never had one; violates the stamp-×2 rule).

16. **[DEFERRED] Auto-halve-on-parse-failure** — engine self-remedy for essay-density whales: on parse failure, retry the chunk at half size instead of logging and moving on. Justified by the 'do'/'position' chunk-12 experience. Deferred S69: documented as a known-good design; candidate cornerstone for a whole new phase of curation, whenever batchcuration re-enters service — an engine that sizes its own bites is a step from operated crank toward self-running pipeline.
18. **[DEFERRED] Academic outreach — instructor one-pager** — materials for a student-exercise pitch to linguistics faculty (use-a-tool framing, not a doctrine play): one-page instructor pitch + a docket of well-chosen families (mixed difficulty — clean cases, a lexophone, a fat-family fragment) + scoring sheet; student-vs-corpus and student-vs-student agreement as the payoff numbers. Deferred S69, sequenced BEHIND a round of less-structured emails, which runs first. Design caution on record: voting was the project's original premise (Murray collecting slips) and was abandoned when the overhead exceeded the structure — any student-ruling mechanism must stay lighter than that lesson, and students are a jury pool, not judges; the desk remains the highest court.
    - **Marketing learnings (S69, banked from academic reply #2 — an engaged decline):** (a) *Targeting map*: the four subfields where the conversation belongs are lexical semantics, psycholinguistics, language acquisition, and computational lexical resources — the last is the prime door (WordNet/FrameNet community: people who build and structurally evaluate corpus artifacts rather than adjudicate theoretical orthodoxy). (b) *Lead with TC/Kuperman, framework second*: the academic's one substantive suggestion — frame core claims as testable hypotheses against the empirical vocabulary-development literature — is exactly what the TC campaign already did; unprompted convergent validation that the opening line of any pitch is "placement order tested against Kuperman AoA norms: 492 corrections, measured debt 808 → 429," with Bellis's Law introduced only after the empirical hook lands. (c) *Engaged declines are targeting data*: a stranger read the material, found it legible, and routed it to the right subfields with warmth — legibility confirmed even where fit wasn't. (d) *Sequencing validated*: reply #2 came from the less-structured approach, supporting informal-first. (e) The infographic serves as the standing follow-up asset for any reply.
19. **[DEFERRED] Structured curation logging — S69 ruling: nothing imperative; banked for when consumers exist.** Filter for all of it: structure pays where the machine half both writes and reads; overhead must never land on the curator (the Murray-votes lesson). (a) *curator_log payload*: add a jsonb `payload` column (or discrete `prev_parent` / `prior_qualifier` / `moved_count` columns); the S69-instrumented pruner verbs currently serialize structured facts into `notes` prose \u2014 cheapest first move, producers and consumers both already live. (b) *votes schema for crank verdicts*: one row per draw per family per placement with verdict + dissent, replacing FLAGS-markdown burial; genuinely aligned with equivocality doctrine but downstream of batchcuration \u2014 build it as the first design task of the batchcuration-reentry phase, sibling of item 16. (c) *murray_slips.notes stays loose text* \u2014 human-authored/audienced; looseness is a feature; machine parses retrospectively if ever needed. (d) *pass_progress as-is* until the reentry phase defines its needs.


20. **[DEFERRED] AI-suggest button on the Qualifier field** — *in plain terms: a small button beside any qualifier input that asks the AI to propose a qualifier for that child–parent pair.* Added S70 at curator direction. Natural home: the pills tray (already the field's action row, alongside the variant pills and the Edit>‘word’ open-pill); the browser-direct Anthropic API path used by batchcuration is the plumbing. Curator adjudicates as always — the button drafts, never saves. Source tagging per AI-origin convention.

21. **[DEFERRED] Visitors worklist: prestige numbers** — *in plain terms: give claimed avatars a fixed claimant number (“visitor #7”) — a permanent, ordinal badge of having identified themselves early.* Added S70 at curator direction. Order presumably by claim time (avatars table has the record); number is assigned once and never reused. Pairs with the open design question banked S70: whether the Visitor List should become a participation census (all avatars with activity counts) — prestige numbers would then distinguish the claimed within it.

## Potential Metrics (dashboard candidates — S66 enumeration, recovered S68)
*The full pool from the S66 sitting. The five cheapest were wired into dashboard v2608171100 (marked ✔ below). Baselines are S66-era and stale — the campaign has since retired blanks and merged duplicates. Candidates, not commitments; promote to numbered items when declared.*

**Doctrine compliance (should trend to zero)**
- Empty-qualifier count — standing Law 4 violations. S66 baseline 108/53 families; corpus now at 0 — as a tile it becomes broom1's live report card.
- Measured TC debt — the tc_suspects_PREVIEW count: slips whose child's Kuperman AoA trails its qualifier's and/or parent's by ≥ the margin ([alt:] stripped, bracket qualifiers and sentinel parents skipped, norms-covered words only). Precision requirements: the tile must state its margin (2.0) and norms count (31,105), and show TWO numbers — debt (data condition; S68 arc: 808 → 438 post-campaign) and runnable (debt minus tc1-certified families; 15 post-campaign). Floor is nonzero BY DESIGN: KEEP-cleared dismissals remain data-suspect forever — the tile gauges the data condition, the certificates gauge adjudication. Moves only when the corpus, the norms, or the margin move.
- ✔ Temporal inversions — wired (tiered ≥1/≥2/≥3).
- ✔ Symilar collisions — wired.
- ✔ Duplicate slips — wired.

**Structure health**
- ✔ Reachability — wired (% words with a chain to a prime).
- Chain completeness — % of words whose entire breadcrumb to a prime has no empty qualifiers: the Address-to-Definition ratio ("how much of the corpus is defined, not just filed").
- ✔ Fan-out profile — wired (≥8/≥12/≥20 families, % of slips inside).
- Coverage by pass key — families certified under phase2 vs familize, plus currently-uncertified (reopened) count. The certificate as a gauge, not just a table.

**Theory metrics (the dashboard as instrument, not gauge)**
- Primitive census — the depth-1 stratum: every slip whose parent is a prime; per-prime fan-out ranking. The primes/primitives doctrine (S68: primes absolute/generated; primitives = products, one subtraction downstream, sharply positioned but variably populated) made queryable. SQL drafted S68; constitution predicts the first six rows (the interrogatives); everything beyond is the corpus's ratified answer to "what did this language build first?"
- Qualifier stratum depth — mean depth of qualifier-words by child depth. S66 baseline flat ~3.2–3.8; the "bounded subtraction lexicon" claim as a live chart — the band should tighten, not drift.
- Qualifier mix by depth — % prime / operator / lexical / out-of-lexicon at each depth. S66: operators climb 8%→40% depth 1→7; bare primes ~1% and falling.
- Out-of-lexicon qualifier rate — qualifiers that aren't corpus words (S66: 22–36% by depth). Each is a missing word wanting insertion or a leak; the trend tells which way the boundary moves.
- Qualifier economy — distinct qualifier vocabulary size, top-20 most-used, reuse curve. Tests whether subtraction runs on a small closed set — the strong form of the stratum claim.
- Synonymy census — committed [synonym] verdicts, total. "True synonymy is rare" with a number attached, updating live.
- Lexon activity index — for givens: parent-uses + qualifier-uses (S66: water 30, food 17, air 12, hammer/dog/egg 0). Leaderboard plus inert-givens list; doubles as the operational lexon test.
- Stranded-lexon levers — qualifier-words ranked by inversions they cause. The repatriation worklist as a standing panel rather than a one-off CSV.
- Sense-tag census — count of (sense)-tagged slips and the lexophone registry size, so the polysemy doctrine has a population figure.


**Removed S70 (curator disposition):** items 11 (fatok verdict), 12 (whale/familization campaign), 13 ('feel' sitting), 17 (qualifier typography) struck at curator direction — "If they're important I'll cite them again." Revival is by citation; full prior text lives in the S69 backlog copy and the S70 transcript.

## Done (retained for provenance)
- **REMOVED S69 — Carried desk debt** (was item 15; named at the fable5-31 gavel). All four debts rest in their session-scoped exports, which are the record: six deadlock-blanks certified-with-∅ ('analyze', 'arrange', 'arrangement', 'behave', 'believe', 'hold' — FLAGS_fable5-31.md); 41 fable5-30 parse-gap slips (desk-or-never — incumbent placements intact, only the certificate missing); 'sexual' (Law 7 head question); 'presentiment' (cycle-guarded). No action owed; any of them can be ruled if it crosses the desk. The backlog carries work, not write-offs.
- **REMOVED S69 — TC residue sittings** (was item 14). Ruling: that's what worklists do — move tasks from development todo lists to the UI. The ~450-row sweep self-tracks in the TC Review (AoA) worklist (self-populating by source=tc-*); no backlog line needed. The ~206-case flags sitting lives in the FLAGS export files on disk — the exports are its record, per the session-scoped-exports doctrine.
- **REMOVED S69 — Outside-lineage held-reparents worklist** (was item 5). Definition banked at removal (the term was never defined in doctrine): reparent verdicts whose proposed new parent lies outside the family under adjudication, held unexecuted because the crank's authority is family-scoped; the item promised an archaeology pass (FLAGS/transcript hunt, census unknown) plus a ruling docket, with the 'do' dispersal routing map as founding docket. Ruling: overkill, displaced by human curation — cross-family misfits surface naturally under the curator's ongoing review; no dedicated collection machinery warranted.
- **REMOVED S69 — Breadcrumb parenting audit** (was item 4). Moved to Jack's own list: it's his ongoing analysis, to be done more generally than the one-shot ~50-chain acceptance test the item specified. Not a Claude-side work item.
- **REPEALED S69 — UI audit-trail parity** (was item 1). The reversibility note served bulk machine commits, where revert paths and dissent trails defend crank verdicts at volume. Hand edits are a different channel — curator-authoritative, deliberate, low-volume, the highest court; instrumenting every UI write path to annotate verdicts that need no defense is surface area for the asgn-sync bug class in exchange for ceremony. The operational need (broom2 staleness) is already served by the updated_at column (S67). Diminishing returns relative to the vision.
- **REPEALED S69 — Show curation notes, index + curate** (were items 9/10, gated on item 1). Ruling: the negotiation upon looking at a concept occurs in the reader's brain in a tenth of a second (and may then drag on) — none of it benefits from the history. The notes are curation infrastructure, not reader content; the trail's audience is the desk, export-side, where it already lives.
- **AoA / Temporal Coherence campaign — COMPLETE S68** (was open item 3). Engine built (AoA · TC Sweep tab), Kuperman norms imported (31,105 words, aoa_norms), pilot → full run → formal zero: 492 tc1 certificates, docket 0, measured TC debt 808 → 429 (nonzero floor by design). Landmark verdicts: break = pause⟨time⟩; guy = man⟨plain⟩; epistemic quartet → think⟨true/sure/picture/without⟩; day/age → time⟨light/long⟩; dark = light⟨[not]⟩. Residue tracked as open item 14.
- **TC Review (AoA) worklist** — index.html v2608242045, S68. Fourteenth worklist room: lists all source=tc-* slips with the engine's full argument; Confirm ratifies (source→cavendish7, tc tag survives in notes), Revert restores the was-placement in one tap (the commit policy's row-by-row revertability, given a face), tap-through to Edit for custom rulings. Also fixed an item-9 straggler: the pipeline worklist's \u2716-escaped qualifier display (escapes were invisible to the literal-✖ grep).
- **Doc-sync batch** — S68. TEST_METHODOLOGY §11 rewritten (daily-automatic suffices) v2608241520; ARCHITECTURE §5/§6 broom-family language + broom2 paragraph v2608241520; broom glossary entries placed as CURATION_CONSTITUTION.md §9 (curator-machinery section, human-facing, explicitly excluded from the engine's embedded copy) v2608241540; standalone fragment retired.
- **Engine log-message wording** — batchcuration v2608241520, S68. Unadjudicated-chunk warning now names both causes (parse failures, API 500s) and the whale-doctrine remedy (smaller chunk, ≤12).
- **dashboard.html recovered + fixed** — S68. v2608171100 (the S66 five-doctrine-tile build, interrupted before browser test) located in the S66 transcript via 'metric' keyword, replayed diff-for-diff onto the identical v2606072620 base; first runtime exposed the hardcoded stale-list defect (five new tiles never cleared Recalculating); fixed with querySelectorAll in render+error paths as v2608241500, deployed and verified.
- **Angle-bracket normalization** — index.html + curate.html v2608241320, S68. All 19 qualifier display sites converted to hugged ⟨⟩ notation; ✖-era and paren-era renderings retired.
- **Typeahead Enter-selects-exact-match** — index.html v2608241340, S68. wireLexTypeahead (13 call sites) + category picker; main lookup already had it via doL exact auto-invoke.
- **Edit-view breadcrumbs stay in Edit** — index.html v2608241340, S68. Crumb click now navigates within Edit (sentinels no-op). Trade-off on record: Concept Pruner no longer reachable from Edit crumbs (Lookup crumbs unchanged).
- **updated_at column + trigger** — added S67. Broom2 unlocked.
- **Chunk-size UI override** — batchcuration v2608211430, S67. Whale doctrine: be requires ≤12.
- **curate.html familize reclassified** — propose-only/read-only; Save off-limits (S67 standing rule).
